---
interface Props {
	title: string
	review: string
	author: string
}

const { title, review, author } = Astro.props
---

<div class="flex flex-col rounded-2xl bg-white p-5 shadow">
	<h4 class="mb-4 text-2xl font-semibold">{title}</h4>
	<p class="mb-2 text-lg">{review}</p>
	<span class="mt-auto text-xl font-bold">{author} 님</span>
</div>
